node_modules/
.DS_Store
*.log
npm-debug.log*
.env
.env.*
# nunca un .npmrc de proyecto (podría contener un token real; el de publish es .npmrc.publish, sin secretos)
.npmrc
# dist/ SÍ se versiona: es generado (npm run build) pero el drift gate de npm test
# garantiza que siempre coincide con tokens/milpa-tokens.json.
